{
  "scripts": {
    "build": "lerna run build",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "rootDir": "packages/",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "lib"
    ]
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.10",
    "jest": "^27.4.7",
    "lerna": "^4.0.0",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.4.0"
  }
}

export type Setting = {
    /**
     * The key of the value.
     *
     * mirror - Key for retrieving mirror enabled/disabled.
     */
    key: 'mirror';
    /**
     * boolean value of the key
     */
    enabled: boolean;
}